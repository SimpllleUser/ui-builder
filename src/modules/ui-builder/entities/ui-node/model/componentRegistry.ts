// Re-exports for backwards compatibility.
// All component metadata lives in componentDefinitions.ts
export {
  getComponentSlots,
  WRAP_CONTAINER_TYPES as WRAP_ALLOWED_TYPES,
} from './componentDefinitions'

export type { PropField, PropertySection, ComponentDef } from './componentDefinitions'
