# UI Builder

Standalone Vue 3 and Vuetify project extracted from `my-core`.

```sh
npm install
npm run dev
```

Open http://localhost:3001. The **Editor** view is the former `/ui-builder` page. **Classic** preserves the earlier component implementation.

Use `npm run build` to type-check and build the project.
